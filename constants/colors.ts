/**
 * Shared color tokens for Rim Ready.
 * Import from here instead of hard-coding hex values in screens/components.
 */

// ── Brand ─────────────────────────────────────────────────────────────────────
export const PURPLE = '#6C47FF';
export const TEAL   = '#3DD9C0';

// ── Auth screen (dark navy theme) ─────────────────────────────────────────────
export const AUTH_BG        = '#1A1744';   // full-screen background
export const AUTH_CARD      = '#252060';   // button / card surface
export const AUTH_TEXT      = '#FFFFFF';   // primary text
export const AUTH_MUTED     = 'rgba(255,255,255,0.45)';  // footer / or-divider
export const AUTH_SUBTLE    = 'rgba(255,255,255,0.65)';  // tagline

// ── Home screen (light theme) ─────────────────────────────────────────────────
export const HOME_BG        = '#F2F2F7';   // page background
export const HOME_CARD      = '#FFFFFF';   // card surface
export const HOME_TEXT      = '#1C1C1E';   // primary text
export const HOME_LABEL     = '#8E8E93';   // secondary / label text

// ── Semantic ──────────────────────────────────────────────────────────────────
export const DANGER         = '#FF3B30';   // destructive actions (logout, delete)
